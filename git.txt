git init
git add *
git config --global user.email "you@example.com"
git config --global user.name "DMCC"
git commit -a -m "primeiro commit"

git status
git branch
git branch dev
git checkout dev

git remote add nameTag httlps://github.com/blabla.git
git push -u nameTag master
